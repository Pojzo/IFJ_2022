#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdboool.h>


#include "expression.h"
#include "parser.h"
#include "stack.h"

bool rule_expr (token_storage_t token_storage){
    return e_derivate;
}

bool e_derivate(token_sotrage_t token_storage){
    token_t token = get_token(token_storage);
    if (token != NULL && token)
}